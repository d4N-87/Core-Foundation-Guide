{
	// English: Inherits the base TypeScript configuration generated by SvelteKit.
	// Italiano: Eredita la configurazione TypeScript di base generata da SvelteKit.
	"extends": "./.svelte-kit/tsconfig.json",

	// English: Overrides and additions to the base compiler options.
	// Italiano: Sovrascritture e aggiunte alle opzioni di base del compilatore.
	"compilerOptions": {
		// English: Allows importing and processing of .js files in the project.
		// Italiano: Permette di importare e processare file .js nel progetto.
		"allowJs": true,
		// English: Enables type-checking inside .js files (usually paired with @ts-check comments).
		// Italiano: Abilita il controllo dei tipi all'interno dei file .js (solitamente in coppia con i commenti @ts-check).
		"checkJs": true,
		// English: Improves compatibility between CommonJS and ES Modules.
		// Italiano: Migliora la compatibilità tra CommonJS e i Moduli ES.
		"esModuleInterop": true,
		// English: Enforces that filenames are cased consistently.
		// Italiano: Forza una coerenza maiuscole/minuscole nei nomi dei file.
		"forceConsistentCasingInFileNames": true,
		// English: Allows importing .json files as modules.
		// Italiano: Permette di importare file .json come moduli.
		"resolveJsonModule": true,
		// English: Skips type checking of all declaration files (*.d.ts).
		// Italiano: Salta il controllo dei tipi di tutti i file di dichiarazione (*.d.ts).
		"skipLibCheck": true,
		// English: Generates corresponding '.map' files for debugging.
		// Italiano: Genera i file '.map' corrispondenti per il debugging.
		"sourceMap": true,
		// English: Enables all strict type-checking options.
		// Italiano: Abilita tutte le opzioni di controllo dei tipi più restrittive.
		"strict": true,
		// English: Explicitly includes Node.js type definitions for server-side code (e.g., `fs`, `path`).
		// Italiano: Include esplicitamente le definizioni dei tipi di Node.js per il codice lato server (es. `fs`, `path`).
		"types": ["node"]
	}
}